import { EmploymentCheckerPipe } from './employment-checker.pipe';

describe('EmploymentCheckerPipe', () => {
  it('create an instance', () => {
    const pipe = new EmploymentCheckerPipe();
    expect(pipe).toBeTruthy();
  });
});
